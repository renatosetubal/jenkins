pipeline{
    agent any
    stages{
        stage('testando a primeira parte da pipeline'){
            steps{
                sh 'cat /etc/fstab | tee /tmp/redirecionamento.txt'
            }
        }
    }
}